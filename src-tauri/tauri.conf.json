{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "PepTrack",
  "version": "0.1.0",
  "identifier": "com.tauri.dev",
  "build": {
    "frontendDist": "../frontend/dist",
    "devUrl": "http://localhost:5173",
    "beforeDevCommand": "bash -lc 'if [ -f ../frontend/package.json ]; then npm --prefix ../frontend run dev -- --host; else npm --prefix ./frontend run dev -- --host; fi'",
    "beforeBuildCommand": "bash -lc 'if [ -f ../frontend/package.json ]; then npm --prefix ../frontend run build; else npm --prefix ./frontend run build; fi'"
  },
  "app": {
    "windows": [
      {
        "title": "PepTrack",
        "width": 1400,
        "height": 900,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
